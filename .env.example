# Django Settings
# Copy this file to .env and adjust values for your environment

# SECURITY WARNING: keep the secret key used in production secret!
# Generate a new key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=django-insecure-uvkz7p7h5s*k26pbgs@1s^-(5%xd8p+(4j(zbd1fa8vh44jztv

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# Comma-separated list of allowed hosts (e.g., 'localhost,127.0.0.1,.example.com')
ALLOWED_HOSTS=127.0.0.1,localhost,testserver

# Database URL (default: SQLite)
# For PostgreSQL: postgres://USER:PASSWORD@HOST:PORT/NAME
# For MySQL: mysql://USER:PASSWORD@HOST:PORT/NAME
DATABASE_URL=sqlite:///db.sqlite3

# Static files configuration
STATIC_ROOT=staticfiles
MEDIA_ROOT=imagens_exames

# Security settings (set to True in production)
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Email configuration (optional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-password
# DEFAULT_FROM_EMAIL=your-email@gmail.com

# Optional: Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn.ingest.sentry.io/your-project